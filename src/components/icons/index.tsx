import Tick from './Tick';
import Drag from './Drag';
import Close from './Close';

const Icon = () => {
  return (
    <></>
  )
}

export default Icon;

Icon.Tick = Tick;
Icon.Drag = Drag;
Icon.Close = Close; 
